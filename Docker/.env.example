# [GUIDE] 이 파일을 복사하여 .env 파일로 이름을 변경하고, 실제 비밀번호 값을 채워 넣으세요.
# Command: cp .env.example .env

# ==========================================
# 1. Database (MySQL)
# ==========================================
# [보안] Root 계정은 초기 세팅용입니다. 앱 구동 시에는 사용을 지양하세요.
MYSQL_HOST=mysql
MYSQL_PORT=3306
MYSQL_ROOT_PASSWORD=change_me_to_strong_password
MYSQL_DATABASE=fraud_guard

# [보안/권장] 어플리케이션 전용 계정 (init_db.py에서 이 정보로 유저가 생성됩니다)
# worker나 consumer는 root가 아닌 이 계정을 써야 안전합니다.
MYSQL_APP_USER=app_user
MYSQL_APP_PASSWORD=change_me_to_app_password

# ==========================================
# 2. Redis
# ==========================================
REDIS_HOST=redis
REDIS_PORT=6379
REDIS_PASSWORD=change_me_to_redis_password

# ==========================================
# 3. Kafka & Topics
# ==========================================
KAFKA_BOOTSTRAP_SERVERS=kafka:9092
KAFKA_TOPIC_RAW=raw-topic
KAFKA_TOPIC_ERROR=err-topic
KAFKA_TOPIC_PROCESSED=2nd-topic
KAFKA_CONSUMER_GROUP_ID=fraud-core-group

# ==========================================
# 4. ML Model Configuration
# ==========================================
# 컨테이너 내부 경로 기준입니다. (수정 불필요)
MODEL_PATH_TIER1=/app/data/ML/tier1model.cbm
MODEL_PATH_TIER2=/app/data/ML/tier2model.cbm

# [튜닝] 모델 임계값 (프로젝트 기본값)
MODEL_THRESHOLD_TIER1=0.99816559
MODEL_THRESHOLD_TIER2=0.56802705

# ==========================================
# 5. Kafka Topic Configuration
# ==========================================
# KAFKA 토픽의 파티션 개수를 통제합니다. (수정 불필요)
KAFKA_PARTITIONS_RAW=20
KAFKA_PARTITIONS_ERR=3
KAFKA_PARTITIONS_PROCESSED=15